# PDF Analyzer Service for local development
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdf-analyzer-service
  namespace: default
  labels:
    app: pdf-analyzer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pdf-analyzer-service
  template:
    metadata:
      labels:
        app: pdf-analyzer-service
    spec:
      containers:
      - name: pdf-analyzer-service
        image: pdf-analyzer-service
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: PORT
          value: "8080"
        - name: COMMIT_SHA
          value: "local-dev"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /v1/health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /v1/health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: pdf-analyzer-service
  namespace: default
  labels:
    app: pdf-analyzer-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: pdf-analyzer-service
